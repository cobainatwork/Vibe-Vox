# Vibe-Vox 環境設定範本。多為選填——ASR 模型（microsoft/VibeVoice-ASR）已 bake
# 進 vllm image，served name（vibevoice）也是 BFF 預設，故正式版開箱即用、無需設定。
# 只有要覆寫預設時才複製為 .env：cp .env.example .env（docker compose 自動讀）。

# （選填）允許的前端 Origin（CORS 白名單）。預設 http://localhost。
# 從遠端機 IP 存取時設為該來源，例如 http://10.0.0.5。
# VIBE_VOX_FRONTEND_ORIGINS=http://localhost

# （選填）呼叫 ASR 的逾時秒數（涵蓋網路 + 模型推論）。預設 300。
#
# **此值決定音檔長度的可用上限**（約 5T − 10 秒，見 docs/api/asr.md §3.3），不是
# 模型的 61 分鐘。調大它必須同步 frontend/nginx.conf 的 proxy_read_timeout，否則
# 反向代理會先切斷、使用者拿到 HTML 錯誤頁而非 JSON 錯誤信封（#35）。
# bff/tests/test_config.py 會實際讀取 nginx.conf 比對，改壞會紅。
# VIBE_VOX_ASR_TIMEOUT_SECONDS=300

# （選填）單次送去對齊的段數上限。預設 8。
#
# **這個值有兩個讀取者，改必須同時改**：BFF 用它決定怎麼分批，aligner 用它決定拒絕
# 什麼。只調 BFF 而不調服務端，超出的批次會換來 400 BATCH_TOO_LARGE；只調服務端而不
# 調 BFF，BFF 會繼續送舊的批量。同一個變數名餵給兩個服務即可（compose 已如此傳遞）。
# bff/tests/test_config.py 會實際讀取 aligner 的 config 比對兩邊的預設值。
#
# **在這台測試機（單張 48 GB、三個模型共用）上不要調大。** 原值 32 是校準出來的，但在真機
# 上 CUDA OOM（#36 的實跑驗收）：批次張量
# 會 pad 到該批最長的段落，而校準用的是同一段音訊重複 32 次、padding 浪費恰好 1 倍，
# 真實錄音則接近 2 倍。日常負載 2-4 段觸不到任何上限，會撞到的只有管理平面的長音檔
# 測試，而那條路多幾次往返只差幾秒。
#
# 卡上的空間主要由 vLLM 決定（見本檔下方的三個參數），調大本值只是把 aligner 往同一個
# 天花板推。三個模型的共存實測與各服務實佔記於 #31。
# VIBE_VOX_ALIGNER_MAX_BATCH_ITEMS=8

# （選填）vLLM 的三個記憶體參數。它們在 docker-compose.yml 有顯式預設，歷次量測記於
# #31。此處只記操作者要知道的部分。
#
# 上下文長度。**它是輸入加輸出的總預算**，vLLM 以 prompt + max_tokens 驗證，超過即回 400。
# 一段 T 秒的音檔約需 `17.5T + 203` tokens（音訊每秒 7.5、max_tokens 為 10T + 100）。
#   24576 → 約 23 分鐘      8192 → 約 7.6 分鐘      4096 → 約 3.7 分鐘
# 預設 24576 使逾時（約 20 分鐘，見 VIBE_VOX_ASR_TIMEOUT_SECONDS）仍是較先觸發的那一層，
# 音檔長度上限的行為不變。**調小會讓長音檔改為立刻回 400 而非等到逾時**，若要調小，
# docs/api/asr.md §3.3 與 frontend/src/asr.ts 的上限也要一起改。
# VIBE_VOX_VLLM_MAX_MODEL_LEN=24576

# 單批最大序列數。對齊 BFF 的 VIBE_VOX_MAX_CONCURRENT_HEAVY_REQUESTS（預設 8）：vLLM 沒有
# 理由準備比呼叫端送得出來還多的序列。
# VIBE_VOX_VLLM_MAX_NUM_SEQS=8

# 記憶體佔用比例。**它決定的是預算，不是 vLLM 的實際佔用**：行程佔用約為「總量 × 本值」
# 再加 1.8 至 2.2 GB 的帳外開銷（CUDA context 等）。要精確值只能量，且**必須等 vLLM 完全
# 起來**，啟動途中的讀數會低估數 GB（2026-08-04 踩過，記於 #31）。
#
# **預設 0.65 是被 TTS 逼出來的，不是 ASR 自己的需求。** 2026-08-05 選 0.70 時只有 ASR 與
# aligner 在卡上，並經逐字元驗證辨識文字與 0.8 相同。2026-08-07 加上 TTS 後 0.70 起不來：
# VoxCPM2 的約束是「啟動當下的 free memory ≥ total × 自己的 utilization」，剩下的空間差
# 一點就在啟動期被拒。降到 0.65 三者才共存。
#
# **0.65 未做 0.70 那樣的逐字元比對**，只有「10 分鐘音檔辨識成功」。utilization 影響的是
# KV cache 大小而非解碼路徑，理論上不改變輸出，但那是推論不是實測。
#
# **只跑 ASR（不啟 tts profile）時可以調回 0.70**，那是有逐字元證據的值。
#
# **再調降前先確認 vLLM 仍起得來**：它需要 KV cache 裝得下一條 max-model-len 的序列，否則
# 拒絕啟動。每 token 約 56 KiB，故 24576 tokens 約需 1344 MiB。
# VIBE_VOX_VLLM_GPU_MEMORY_UTILIZATION=0.65

# （選填）TTS 的兩個記憶體參數。**與 vLLM 的機制不同，不要照抄那邊的直覺。**
#
# gpu_memory_utilization 是 per-instance 上限，不是同卡瓜分的份額——官方 docstring 明寫
# 另一個 vLLM 實例在同卡上並不影響它。真正的約束是啟動當下的 free memory：不足即
# `raise ValueError` 拒絕啟動，不會拖到推論期才 OOM。
#
# 2026-08-07 實測（遠端 GPU 機，ASR 降到 0.65 且 aligner 在位）：**0.17 起得來**，行程實佔
# 7952 MiB，`nvidia-smi` 上 GPU 0 隨後剩 2659 MiB。
#
# **不要拿實佔去反推 utilization 校準得多準。** 預算的分母有兩個值——`nvidia-smi` 報
# 46068 MiB、torch 報 45465 MiB——兩者算出的預算差 103 MiB，而 voxcpm2 傳輸選型那份 spec
# §3.1.8 已結論「餘裕的精確值算不出來也不該算」，理由正是那 603 MiB 差距大於任何算得出
# 來的餘裕。這裡只記實佔，不記差額。
#
# 這個數也**不能拿來排序**先前三個來源（官方模型卡 ~8 GB 估算、上游 yaml 自述 peak
# ~13 GiB、recipe 的過期數字）：7952 是 KV 被顯式封頂在 1 GiB 之下的**閒置常駐**佔用，
# 而 yaml 說的是推論峰值，量的不是同一件事。那三個來源仍然都不能拿來規劃本機容量。
#
# **這只證明服務起得來，不證明合成不會 OOM**：尚未打過 POST /api/tts/speech，推論期的
# activation 峰值仍未知，而餘裕只有 2659 MiB。合成路徑實測歸 #31。
# VIBE_VOX_TTS_GPU_MEMORY_UTILIZATION=0.17

# KV cache 的顯式上限（bytes，預設 1 GiB）。設了它之後 KV 尺寸**完全不看**
# gpu_memory_utilization——上游 log 逐字寫著 "This does not respect the
# `gpu_memory_utilization` config"。上游 voxcpm2.yaml 的預設是 6 GiB，在本機加上權重
# 與 talker 緩衝會超過剩餘空間。1 GiB 在 2026-08-07 的實測中起得來（見上一項）。
# VIBE_VOX_TTS_KV_CACHE_BYTES=1073741824

# （選填）單次合成的文字長度上限（字元），預設 2000。超過回 413 INPUT_TOO_LONG。
# 消費端是逐句合成，實際請求遠低於此；上限擋的是整篇文章被灌進來而長時間佔住 GPU。
# VIBE_VOX_TTS_MAX_INPUT_CHARS=2000

# （選填）呼叫 TTS 的逾時秒數，預設 120。**尚未以實測校準**（延遲 bar 是 #17，服務跑起來
# 才量得到）；此值為覆蓋冷啟的保守估計，穩態一句話遠低於它。
# VIBE_VOX_TTS_TIMEOUT_SECONDS=120

# 本機無 GPU 開發用 docker-compose.dev.yml（BFF 以 stub 啟動、回假辨識），不需本檔。
