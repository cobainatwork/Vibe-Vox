# Vibe-Vox 環境設定範本。多為選填——ASR 模型（microsoft/VibeVoice-ASR）已 bake
# 進 vllm image，served name（vibevoice）也是 BFF 預設，故正式版開箱即用、無需設定。
# 只有要覆寫預設時才複製為 .env：cp .env.example .env（docker compose 自動讀）。

# （選填）允許的前端 Origin（CORS 白名單）。預設 http://localhost。
# 從遠端機 IP 存取時設為該來源，例如 http://10.0.0.5。
# VIBE_VOX_FRONTEND_ORIGINS=http://localhost

# （選填）呼叫 ASR 的逾時秒數（涵蓋網路 + 模型推論）。預設 300。
#
# **此值決定音檔長度的可用上限**（約 5T − 10 秒，見 docs/api/asr.md §3.3），不是
# 模型的 61 分鐘。調大它必須同步 frontend/nginx.conf 的 proxy_read_timeout，否則
# 反向代理會先切斷、使用者拿到 HTML 錯誤頁而非 JSON 錯誤信封（#35）。
# bff/tests/test_config.py 會實際讀取 nginx.conf 比對，改壞會紅。
# VIBE_VOX_ASR_TIMEOUT_SECONDS=300

# （選填）單次送去對齊的段數上限。預設 8。
#
# **這個值有兩個讀取者，改必須同時改**：BFF 用它決定怎麼分批，aligner 用它決定拒絕
# 什麼。只調 BFF 而不調服務端，超出的批次會換來 400 BATCH_TOO_LARGE；只調服務端而不
# 調 BFF，BFF 會繼續送舊的批量。同一個變數名餵給兩個服務即可（compose 已如此傳遞）。
# bff/tests/test_config.py 會實際讀取 aligner 的 config 比對兩邊的預設值。
#
# **在這台測試機（單張 48 GB、三個模型共用）上不要調大。** 原值 32 是校準出來的，但在真機
# 上 CUDA OOM（#36 的實跑驗收）：批次張量
# 會 pad 到該批最長的段落，而校準用的是同一段音訊重複 32 次、padding 浪費恰好 1 倍，
# 真實錄音則接近 2 倍。日常負載 2-4 段觸不到任何上限，會撞到的只有管理平面的長音檔
# 測試，而那條路多幾次往返只差幾秒。
#
# 卡上的空間主要由 vLLM 決定（見本檔下方的三個參數），調大本值只是把 aligner 往同一個
# 天花板推。三個模型能否共存的算術記於 HANDOFF.md 的 8.2，追蹤於 #31。
# VIBE_VOX_ALIGNER_MAX_BATCH_ITEMS=8

# （選填）vLLM 的三個記憶體參數。它們在 docker-compose.yml 有顯式預設，理由與量測見
# HANDOFF.md 的 2.4。此處只記操作者要知道的部分。
#
# 上下文長度。**它是輸入加輸出的總預算**，vLLM 以 prompt + max_tokens 驗證，超過即回 400。
# 一段 T 秒的音檔約需 `17.5T + 203` tokens（音訊每秒 7.5、max_tokens 為 10T + 100）。
#   24576 → 約 23 分鐘      8192 → 約 7.6 分鐘      4096 → 約 3.7 分鐘
# 預設 24576 使逾時（約 20 分鐘，見 VIBE_VOX_ASR_TIMEOUT_SECONDS）仍是較先觸發的那一層，
# 音檔長度上限的行為不變。**調小會讓長音檔改為立刻回 400 而非等到逾時**，若要調小，
# docs/api/asr.md §3.3 與 frontend/src/asr.ts 的上限也要一起改。
# VIBE_VOX_VLLM_MAX_MODEL_LEN=24576

# 單批最大序列數。對齊 BFF 的 VIBE_VOX_MAX_CONCURRENT_HEAVY_REQUESTS（預設 8）：vLLM 沒有
# 理由準備比呼叫端送得出來還多的序列。
# VIBE_VOX_VLLM_MAX_NUM_SEQS=8

# 記憶體佔用比例。**它決定的是預算，不是 vLLM 的實際佔用**：行程佔用約為「總量 × 本值」
# 再加 1.8 至 2.2 GB 的帳外開銷（CUDA context 等）。要精確值只能量，且**必須等 vLLM 完全
# 起來**，啟動途中的讀數會低估數 GB（HANDOFF.md 2.4 記著踩過的坑）。
#
# 0.70 已實測驗證：辨識文字與 0.8 逐字元相同（量測數字見 HANDOFF.md 2.4）。
#
# **再調降前先確認 vLLM 仍起得來**：它需要 KV cache 裝得下一條 max-model-len 的序列，否則
# 拒絕啟動。每 token 約 56 KiB，故 24576 tokens 約需 1344 MiB，而 0.70 給的 5.52 GiB 是它
# 的 4.2 倍。
# VIBE_VOX_VLLM_GPU_MEMORY_UTILIZATION=0.70

# 本機無 GPU 開發用 docker-compose.dev.yml（BFF 以 stub 啟動、回假辨識），不需本檔。
