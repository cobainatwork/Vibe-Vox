# Vibe-Qwen 環境設定範本。複製為 .env 後填入實際值：cp .env.example .env
# docker compose 會自動讀取同目錄的 .env 填入 ${VAR}。.env 不進版控。

# ── ASR（遠端 GPU / 正式 compose 需要）──────────────────────────
# vLLM serve 的 VibeVoice-ASR 模型 id（HuggingFace 路徑或掛載的本地路徑）。
# 唯一「必填」項；vllm 與 bff 都用它（同一 id）。
VIBE_QWEN_ASR_MODEL=

# （選填）覆寫 ASR 服務位址。預設 http://vllm:8000（compose 內部網路）。
# 只有 bff 與 vllm 不在同一 compose、要連外部 vLLM 時才需設。
# VIBE_QWEN_ASR_BASE_URL=http://vllm:8000

# （選填）呼叫遠端 ASR 的逾時秒數（涵蓋網路 + 模型推論）。預設 120。
# VIBE_QWEN_ASR_TIMEOUT_SECONDS=120

# ── 前端 ──────────────────────────────────────────────────────
# （選填）允許的前端 Origin（CORS 白名單）。預設 http://localhost。
# 若從遠端機 IP 存取，設為該來源，例如 http://10.0.0.5。
# VIBE_QWEN_FRONTEND_ORIGINS=http://localhost

# 本機無 GPU 開發不需本檔：docker-compose.dev.yml 已設 USE_STUB_MODELS=true，
# 以假辨識啟動、不連真實模型。本檔僅供正式（遠端 GPU）compose。
